{
  "name": "@adivina/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute adivina-la-palabra --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute adivina-la-palabra --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
